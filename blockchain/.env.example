# Flare Coston2 deployer account — copy this file to .env and fill in.
# DANGER (dev.flare.network): never share private keys, never commit them to
# git, never put them in source code. .env is gitignored.
DEPLOYER_PRIVATE_KEY=

# Optional: Blockscout API key for `hardhat verify` against Coston2 explorer.
# FLARE_EXPLORER_API_KEY=
